services:
    frontend:
        build: .
        ports:
            - '127.0.0.1:8080:80'
        volumes:
            - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
        depends_on:
            - server
        restart: unless-stopped
